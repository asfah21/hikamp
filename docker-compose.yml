services:
  mbti-app:
    build: .
    container_name: hikamp
    restart: always
    ports:
      - "127.0.0.1:8081:8080"
    environment:
      - GIN_MODE=release
      - TZ=Asia/Jakarta
      - DATABASE_URL=${DATABASE_URL}
    networks:
      - app-network

networks:
  app-network:
    external: true
